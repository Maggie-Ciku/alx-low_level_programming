#include <stdio.h>
#include "main.h"
/**
 * _isupper - Starting point
 * @c: alphabet
 * Description: checks for uppercase character.
 * Return: 1 (uppercase). 0 (otherwise)
 */
int _isupper(int c)
{
}