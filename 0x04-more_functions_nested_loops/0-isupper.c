#include <stdio.h>
#include "main.h"
/**
 * _isupper - Starting point
 * @c: alphabet
 * Description: checks for uppercase character.
 * Return: Always 0
 */
int _isupper(int c);
{
	return (c >= 'A' && c <= 'Z');
}
