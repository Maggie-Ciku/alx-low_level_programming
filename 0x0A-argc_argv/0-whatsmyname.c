#include <stdio.h>
/**
 * main - Starting point
 * Description: writes its name
 *  @argc: argument count
 *  @argv: array of pointers to argument strings
 *  Return: 0
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
