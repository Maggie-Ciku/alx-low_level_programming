#include "main.h"
/**
 * _islower - Starting point
 *
 * Description: Checks for lowercase character.
 *
 * Return: 1 (Lower case) . 1 (Otherwise)
 */
int _islower(void)
{
	int c;

	if (c >= a && c <= z; c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n')
}
